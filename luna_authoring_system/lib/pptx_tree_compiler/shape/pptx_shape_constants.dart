/// =================================================================================================
/// XML ELEMENTS CONSTANTS FOR PPTX SHAPE TREE PARSER
/// =================================================================================================
/// This file contains the XML element constants used in the PptxShapeTreeParser class.
///
/// The XML element for the connection shape element in a PowerPoint presentation.
const String eConnectionShape = 'p:cxnSp';

/// The XML element for the shape properties within a shape.
const String ePictureShape = 'p:pic';

/// The XML element for the textbox shape element in a PowerPoint presentation.
const String eTextboxShape = 'p:sp';

/// The XML element for the non visual properties for a shape.
const String eNvSpPr = 'p:nvSpPr';

/// The XML element for the the non visual properties for objects.
const String eNvPr = 'p:nvPr';

/// The XML element for the placeholder shape.
const String ePlaceholder = 'p:ph';