import 'package:flutter/material.dart';

class NeedHelpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Need Help'),
      ),
      body: const Center(
        child: Text('Need Help Page'),
      ),
    );
  }
}
